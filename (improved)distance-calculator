# Ask the user to enter the value for distance in kilometers
x=float(input("Enter distance in kilometers: "))
y = 0.621371
z = x*y
print("Distance in miles:",z)

# Ask the user whether they want to convert another distance to miles
a = input("Do you want to convert another distance? (yes/no): ")
#Ask the user to enter the value for distance in kilometers
if a=="yes":
    q = float(input("Enter distance in kilometers: "))
    r = q*y
    print("Distance in miles:",r)
else:
    print("Program ended.")

https://www.programiz.com/online-compiler/2O0S8k4jaK6yE
